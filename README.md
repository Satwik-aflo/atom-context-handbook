# ATOM Context Handbook

Public GitHub Pages mirror for the ATOM context handbook.

The published site is intentionally small: `index.html` is the generated handbook, and `.nojekyll` tells GitHub Pages to serve it as plain static HTML.
